# Python
*.pyc
__pycache__/
*~
.cache
.pytest_cache
*.mo
*.pot

# Django specific
*.log
local_settings.py  # Used for local-specific configuration and secrets
db.sqlite3         # Default Django database file
*.sqlite3
# Environment variables
.env
.env.*
!.env.example
venv/
.venv/
env/
ENV/

# IDE-specific files (optional, can also be global)
.vscode/
.idea/

# Operating System files
.DS_Store
Thumbs.db

# Media and Static files (adjust as needed)
# It is common to ignore locally collected static files, but sometimes they are tracked for deployment.
# The media folder usually contains user-uploaded content and is often ignored and handled by the deployment environment.
htmlcov/
.tox/
media/
# staticfiles/

# Tailwind CLI binary (40MB, download via build_tailwind.ps1)
tailwindcss.exe